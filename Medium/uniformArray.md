# Construct Uniform Parity Array II

## Problem Statement

You are given an array `nums1` containing `n` distinct positive integers.

Construct another array `nums2` of the same length such that all elements of `nums2` are either:

- All odd, or
- All even

For every index `i`, you must choose exactly one operation:

1. Keep the original element:

```text
nums2[i] = nums1[i]
